<%= form_for @programme, :html => {:class => 'form-stacked'} do |f| %>
<% if @programme.errors.any? %>
<div id="error_explanation">
     <h2><%= pluralize(@programme.errors.count, "error") %> prohibited this programme from being saved:</h2>
     <ul>
     	<% @programme.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
	<%end%>
  </ul>
   </div>
<% end %>
<div class="field">
	<%= f.label :programme_name_en, "Programme Name EN" %>
	<%= f.text_field :programme_name_en, :value => @programme.programme_name_en ? @programme.programme_name_en : "Not available" %>
</div>

<div class="field">
	<%= f.label :programme_name_fr, "Programme Name FR"%>
	<%= f.text_field :programme_name_fr, :value => @programme.programme_name_fr ? @programme.programme_name_fr : "Pas disponible" %>
</div>

<div class="field">
	<%= f.label :programme_name_es, "Programme Name ES" %>
	<%= f.text_field :programme_name_es, :value => @programme.programme_name_es ? @programme.programme_name_es : "No está disponible" %>
</div>

<%= f.hidden_field :course_id, :value => @course.nil? ? @programme.course_id : @course.id %>

<div class="actions">
	<%= f.submit :class => 'btn primary' %>
</div>
<% end %>